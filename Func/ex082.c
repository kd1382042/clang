#include<stdio.h>
#include<string.h>
void sort(int* array, int size, int order);
main()
{
	int data[8] = { 6,10,8,2,9,5,1,7 };
	int i;
	char str[256];
	printf("ソート方向を入力:");
	get(str);
	if(str)
}//じかいにつづく（適当）